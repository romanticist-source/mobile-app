import { StyleSheet } from 'react-native';
import {
  background,
  surface,
  border,
  textPrimary,
  textSecondary,
  accentLight,
  xs,
  sm,
  md,
  lg,
  xl,
  fontMd,
  fontLg,
  fontXxl,
  fontXxxxl,
  radiusFull,
  iconContainerSize,
  iconSize,
  headerButton,
} from '@/styles/tokens';

export const styles = StyleSheet.create({
 
});
